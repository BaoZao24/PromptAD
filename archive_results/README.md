# 历史归档说明

截至 2026-07-15，历史实验的 raw result 和 checkpoint 已清理，不作为当前方案的复现入口。

本目录只保留少量历史资料：

- `archived_docs/`：历史实验说明、结果索引和选择报告；
- `archived_analysis_outputs/`：少量历史汇总表。

当前方案、主结果和复现入口只看仓库根目录下的 `docs/`、`tools/`、`analysis_outputs/` 和 `references/`，不依赖本目录。
