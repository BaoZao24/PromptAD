# 现有方案介绍

## 1. 方案定位

当前项目的主线方案为 **`morph_fusion_dualgrad`**。

它是一种面向频谱异常检测的**通用特征提取方法**，核心目标是在**不依赖异常类型先验**的前提下，统一增强频谱图中的多类异常线索。

这里不再采用“按异常类型切换特征提取器”的方案。原因很直接：真实生产环境中的异常类型通常未知，因此最终方法必须在不依赖异常类型先验的前提下统一处理输入。

## 2. 方法定义

当前方案采用统一三通道输入：

```text
morph_fusion_dualgrad = weak_residual + time_gradient + freq_gradient
```

三通道分别承担不同作用：

| 通道 | 作用 | 设计原因 |
|---|---|---|
| `weak_residual` | 强化相对背景的弱偏离 | 适合保留微弱异常线索，尤其对弱扰动更敏感 |
| `time_gradient` | 强化时间方向结构变化 | 用于突出突发边界、时间局部变化与轨迹变化 |
| `freq_gradient` | 强化频率方向结构变化 | 用于突出频带边界、轮廓和频率维结构变化 |

这种设计不是为了某一类异常单独最优，而是为了给每张未知异常频谱图统一提供：

- 弱异常偏离信息；
- 时间维结构信息；
- 频率维结构信息。

## 3. 为什么它更合理

此前的探索说明，不同异常对不同特征表示确实存在偏好，但如果把这种偏好直接写成最终方法，会遇到一个根本限制：

> 在开放异常检测场景中，推理前并不知道当前图像属于哪种异常，因此不能依赖异常标签来决定前端特征提取方式。

`morph_fusion_dualgrad` 避开了这个问题。它不需要先判断异常类别，而是对每张图统一构造同一种融合表示，让模型自行利用残差和梯度线索。

因此，它作为现有方案更接近真实生产环境的使用逻辑。

## 4. 当前最硬的自测结果

当前已经拿到完整、自洽的自测四数据集 baseline 对比结果。活动总表见：

- `analysis_outputs/all_anomaly_summary_tables/selftest_all_anomalies_baseline_rgb.csv`
- `analysis_outputs/all_anomaly_summary_tables/selftest_all_anomalies_morph_fusion_dualgrad.csv`
- `analysis_outputs/all_anomaly_summary_tables/selftest_morph_fusion_dualgrad_vs_baseline.csv`

结果如下：

| 数据集 | Baseline | morph_fusion_dualgrad | 提升幅度 |
|---|---:|---:|---:|
| `burst_signal` | 86.3117 | 89.8767 | +3.5650 |
| `chirp_signal` | 78.7308 | 84.8008 | +6.0700 |
| `dsss_signal` | 89.5625 | 95.8283 | +6.2658 |
| `wideband_pulse` | 96.1333 | 92.6858 | -3.4475 |
| **Overall** | **87.6846** | **90.7979** | **+3.1133** |

## 5. 当前阶段结论

基于现有证据，当前可以确认：

1. `morph_fusion_dualgrad` 在整体上**明显优于 RGB baseline**；
2. 它作为**通用方案**更符合未知异常类型下的生产部署逻辑；
3. `burst`、`chirp`、`DSSS` 上提升明确；
4. `wideband_pulse` 当前仍然下降，说明这套统一融合表示还有进一步优化空间。

## 6. 公开数据评估状态

**公开数据补充评估待补齐。**

当前阶段不应杜撰 `morph_fusion_dualgrad` 在公开数据上的完整结论。现阶段可以确认的是：

- 公开数据 baseline 主表仍然保留在 `analysis_outputs/all_anomaly_summary_tables/rf_spe_png_all_anomalies_baseline_rgb.csv`；
- `morph_fusion_dualgrad` 的公开数据完整补充评估还需要后续继续跑完；
- 因此当前主结论应以自测四数据集结果为准。
