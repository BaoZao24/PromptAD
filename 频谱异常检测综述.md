# 面向 PromptAD 的频谱异常检测文献综述与改进路线

> 写作时间：2026-05-17  
> 目标：为当前 PromptAD 频谱异常检测分支引入外部文献中的技术路线，形成可落地的改进方案。  
> 当前任务设定：按场景分别训练、分别测试，即每个 scene 使用自身的正常样本训练，并在同一 scene 上测试正常与异常样本；当前不再以 cross-site 作为主实验协议。

## 1. 背景与问题定义

当前仓库已经把 PromptAD 从工业视觉异常检测扩展到射频频谱图异常检测。核心流程仍然是：

1. 用 CLIP 图像编码器提取全局图像特征和局部 patch 特征；
2. 用 PromptLearner 学习 normal / abnormal 文本提示；
3. 用正常样本构建视觉 feature gallery；
4. 推理时计算文本异常分数和视觉最近邻异常图。

这个框架的优势是少样本、只需要正常样本、实现成本低。但频谱异常检测和工业缺陷图像存在明显差异：

- 频谱图不是自然图像，横轴/纵轴通常对应时间和频率，局部形状具有物理含义；
- 异常类型有明确时频结构，例如 burst 是短时窄带脉冲，chirp 是斜向扫频轨迹，DSSS 是宽带扩频结构；
- 不同场景之间背景噪声、占用模式、噪声底、设备响应会变化，但当前主实验协议不再直接评估跨站点迁移，而是优先在各 scene 内部验证方法是否有效；
- 低 JSR 异常更接近正常背景，靠自然图像 CLIP 语义很难直接区分；
- 当前 `cls` 图像级 AUROC 主要来自文本异常分数，视觉分支更多用于 score map，可进一步利用正常频谱背景建模。

因此，改进方向不应只停留在“把 prompt 换成 RF 词汇”，而应引入频谱异常检测中的正常背景建模、时频结构建模、重建/预测式异常检测，以及 VLM 异常检测中的 object-agnostic prompt 和多模板融合。

## 2. 相关文献脉络

### 2.1 频谱异常检测：从 PSD 建模到时频图深度模型

早期无线频谱异常检测通常围绕功率谱密度、能量检测、占用统计和阈值规则展开。这类方法可解释性强、部署简单，但在复杂场景下容易受噪声底、站点差异和低功率干扰影响。

SAIFE 是频谱异常检测中较有代表性的深度无监督方法。Rajendran 等提出 Spectrum Anomaly Detector with Interpretable FEatures，使用 Adversarial Autoencoder 对 PSD 数据建模，在无监督条件下完成异常检测和定位，并尝试学习带宽、中心频率、信号类别等可解释因子。它对 PromptAD 的启发是：频谱异常检测不应只依赖“像不像异常”的语义分数，还应显式关注中心频率、带宽、能量分布和局部频段结构。

另一类方法将频谱监测看成时空预测问题。Deep Predictive Coding Neural Network for RF Anomaly Detection 把连续频谱监测图像序列类比为视频预测，通过预测正常频谱演化，再用预测误差检测异常。Peng 等的 spatio-temporal network prediction 也强调利用多时间尺度历史频谱数据做预测和异常检测。这类方法适合连续采集数据，启发 PromptAD 在有序帧或连续切片可用时增加“正常背景预测分支”。

近年来也出现了面向 RF 异常检测的数据集和目标检测式基线。WASD 提供真实 LTE/5G 背景叠加 tone / chirp / pulse 等异常的 spectrogram 样本，并给出异常区间或框标注。这说明频谱异常天然具有局部目标检测属性：异常往往是时频图上的一段线、一个窄带块、一个宽带区域或一个能量突变区域。因此 PromptAD 的 pixel/patch 级异常图应被更充分地用于图像级分数，而不是只作为可视化输出。

### 2.2 通用视觉异常检测：正常 patch 记忆库与重建误差

工业视觉异常检测中，PatchCore、PaDiM、SPADE 等方法证明了正常 patch 记忆库的有效性。PatchCore 的关键思想是保留正常样本的局部特征集合，并通过 coreset 减少冗余，在推理时用最近邻距离衡量局部异常程度。当前 PromptAD 的 visual gallery 与 PatchCore 思路相近，但还比较简单：它没有做 coreset、没有区分频率位置、也没有将 patch 异常聚合进图像级分数。

重建类方法如 DRAEM、Reverse Distillation、FastFlow 等提供了另一条思路：只学习正常样本分布，异常区域会在重建误差、学生-教师特征差异或 flow likelihood 中暴露出来。对频谱图而言，重建类方法尤其有价值，因为频谱异常常表现为局部能量结构破坏；正常背景重建误差可以作为文本分支之外的独立证据。

### 2.3 视觉语言异常检测：从模板 prompt 到异常语义 prompt

CLIP 提供图像和文本对齐空间，使异常检测可以通过 normal / abnormal 文本提示构建判别方向。CoOp 证明了可学习上下文 token 能提升 CLIP 在下游任务上的适配能力，CoCoOp 进一步提出根据输入图像动态生成条件化 prompt，提升跨域泛化。

WinCLIP 将 CLIP 用于零样本/少样本异常分类和分割，强调两点：多模板 prompt ensemble，以及 window/patch 级局部特征聚合。PromptAD 则进一步面向 few-shot anomaly detection，只用正常样本学习 prompt，通过 normal prompt 与 abnormal suffix 的语义拼接构造异常提示，并引入显式异常 margin。

AnomalyCLIP 的关键贡献是 object-agnostic prompt learning：它认为 CLIP 容易关注对象类别语义，而异常检测更需要关注 normality / abnormality 本身。这个观点对本项目仍有启发，因为 `WeaponMuseum_spectrum`、`Playground_spectrum` 这类地点名本身并不是异常语义，真正重要的是“正常频谱背景”和“异常时频结构”。

## 3. 对当前 PromptAD 的诊断

结合当前代码和已有 `improve.md`，PromptAD 频谱分支已经做了两类改进：

- `--prompt-mode rf`：把类别统一成 `radio frequency spectrogram`，并为 burst / chirp / dsss 增加结构化异常描述；
- `--input-mode spectral_gradient`：把输入三通道改为灰度强度、时间方向梯度、频率方向梯度。

这些改动是合理的，但仍有几个短板：

1. 图像级异常分数主要依赖 textual anomaly score，视觉 gallery 的 patch 异常信息没有充分进入 Image-AUROC；
2. prompt 已经有 RF 词汇，但还没有形成“任务域、异常类型、场景背景、噪声强度”的层次化组合；
3. visual gallery 仍是普通 CLIP patch 最近邻，没有频率位置约束、带宽/能量特征，也没有 PatchCore 式 coreset；
4. 没有引入重建/预测式正常背景建模，低 JSR 异常可能被 CLIP 语义分支忽略；
5. 场景背景差异和不同异常结构之间的关系还没有显式处理，模型可能把背景纹理当成异常线索，或者忽略真正的异常结构。

## 4. 可引入的方法与落地方案

### 4.1 结构化 RF prompt：从“异常频谱”到“时频结构异常”

参考来源：WinCLIP、PromptAD、AnomalyCLIP、CoOp/CoCoOp。

当前 `PromptAD/ad_prompts.py` 已经加入 burst / chirp / dsss 的结构词，但可以进一步拆成四层：

1. 任务域层：`radio-frequency spectrogram`, `time-frequency power map`, `wireless spectrum occupancy map`;
2. 正常背景层：`stable noise floor`, `regular background spectrum`, `normal channel occupancy`;
3. 异常结构层：burst / chirp / dsss 对应的局部形态；
4. 场景条件层：不同站点或不同 JSR 下的背景复杂度。

建议 prompt 设计如下：

```text
normal:
a normal radio-frequency spectrogram with stable noise floor
a clean time-frequency power map with regular background occupancy
a normal wireless spectrum patch without injected interference

burst abnormal:
a radio-frequency spectrogram with a short-duration narrowband pulse
a time-frequency power map with a transient burst interference
a wireless spectrum patch with a hidden low-power narrowband burst

chirp abnormal:
a radio-frequency spectrogram with a diagonal chirp interference trace
a time-frequency power map with a slanted frequency-sweeping line
a wireless spectrum patch with an abnormal swept-frequency signal

dsss abnormal:
a radio-frequency spectrogram with disrupted wideband spread-spectrum structure
a time-frequency power map with uneven wideband spectral density
a wireless spectrum patch with abnormal wideband noise-like interference
```

落地位置：

- `PromptAD/ad_prompts.py`：扩展 prompt 库，区分 domain prompt、normal prompt、type prompt；
- `PromptAD/model.py`：支持 prompt group，加权或分别编码后融合；
- `train_cls.py`：增加 ablation 参数，例如 `--prompt-template structured|object_agnostic|scene_conditioned`。

预期收益：

- 提升 chirp、burst 这类形态明确异常的文本可分性；
- 减少模型对站点类别名的依赖；
- 为后续 prompt ensemble 和条件 prompt 留出接口。

风险：

- prompt 数量过多可能稀释异常原型；
- CLIP 对专业 RF 术语理解有限，英文模板要保持视觉形态词和 RF 词并存，例如 `diagonal line`, `narrowband pulse`, `wideband band`。

### 4.2 Object-agnostic + scene-conditioned prompt

参考 AnomalyCLIP 和 CoCoOp，可以把 prompt 从“某个类别的异常”改成“任意频谱图中的正常/异常”。对本项目而言，类别名多为站点名，直接用类别名容易引入无关语义。因此建议使用 object-agnostic RF prompt：

```text
a normal radio-frequency spectrogram
an anomalous radio-frequency spectrogram
a normal time-frequency power map
an abnormal time-frequency power map
```

进一步可以加入场景条件，但不要让场景名直接进入 prompt，而是映射成背景描述：

```text
WeaponMuseum_spectrum -> indoor spectrum scene with relatively stable background
Playground_spectrum -> open outdoor spectrum scene with sparse background activity
TimeSquare_spectrum -> crowded urban spectrum scene with complex background occupancy
Gymnasium_spectrum -> semi-enclosed spectrum scene with moderate background activity
```

落地方案：

- 在 `ad_prompts.py` 中增加 `scene_prompt_mapping`；
- `PromptLearner` 接收 `class_name` 后拼接背景描述；
- 先只在文本分支做 prompt 条件化，不改图像编码器。

实验建议：

- 比较 `rf`、`object_agnostic_rf`、`scene_conditioned_rf`；
- 每组都固定 `input_mode=spectral_gradient`，避免输入变化干扰 prompt 结论。

### 4.3 频谱专用输入表示：不只 RGB，而是物理含义通道

当前 `spectral_gradient` 已经把输入变成强度、时间梯度、频率梯度。这与频谱任务是匹配的，因为 burst 更依赖时间局部变化，chirp 更依赖斜向梯度，DSSS 更依赖宽带纹理。

可以继续扩展为三类输入：

1. `spectral_gradient`：灰度强度 + 时间梯度 + 频率梯度；
2. `log_power_multiscale`：log-power + 局部均值去除 + 局部方差/纹理；
3. `signal_adaptive`：burst/chirp 使用梯度，DSSS 使用 log-power + 宽带能量统计。

具体建议：

- 对每张频谱图做 per-image 或 per-frequency-bin 归一化，降低站点噪声底差异；
- 增加频率方向背景扣除：`x - median(x, time)` 或局部均值扣除；
- 为 DSSS 增加宽带能量均匀性通道，避免纯梯度输入破坏宽带扩频纹理；
- 保留 `rgb` 作为 baseline，所有新输入都必须做 ablation。

落地位置：

- `PromptAD/model.py` 中的 transform；
- 或在 `datasets/` 加载图像后生成多通道 tensor。

### 4.4 PatchCore 式正常频谱记忆库

当前 visual gallery 已经用正常 patch 特征做最近邻，但还可以引入 PatchCore 的三点思想：

1. coreset：减少正常 patch 冗余，提高速度并降低背景噪声过拟合；
2. 多层特征：融合浅层局部纹理和深层语义特征；
3. 图像级分数聚合：用 top-k patch 异常分数作为 image score 的一部分。

频谱任务还应增加位置约束。普通图像中 patch 的语义位置不一定固定，但频谱图的纵轴频率位置有物理意义。因此可以让测试 patch 只和相同或相邻频率带的正常 patch 比较：

```text
普通最近邻：
score(p) = min distance(p, all normal patches)

频率约束最近邻：
score(t, f) = min distance(p(t, f), normal patches with frequency index in [f-r, f+r])
```

这样可以避免模型用低频背景 patch 匹配高频异常 patch，提升局部异常定位稳定性。

落地位置：

- `PromptAD/model.py::build_image_feature_gallery`：保存 patch 的频率索引；
- `calculate_visual_anomaly_score`：加入频率窗口约束；
- 新增 `--gallery-mode global|freq_window|patchcore_coreset`。

图像级融合建议：

```text
image_score = alpha * textual_score
            + beta  * mean(top_k(visual_patch_scores))
            + gamma * max(visual_patch_scores)
```

这是当前最值得优先做的改动之一，因为它直接修复“视觉分支没有进入 Image-AUROC”的问题。

### 4.5 重建/预测式正常背景分支

参考 SAIFE、CAE spectrum sensing、DRAEM、Reverse Distillation、FastFlow 和 RF 时空预测方法，可以为 PromptAD 增加一个轻量辅助分支：

- 输入：频谱图或 CLIP patch feature map；
- 训练：只用正常样本；
- 输出：重建误差、特征重建误差或预测误差；
- 融合：与 PromptAD 文本分数和 visual gallery 分数融合。

三种可选实现：

1. 图像级小型卷积 autoencoder：实现最简单，适合先做 baseline；
2. patch feature autoencoder：重建 CLIP patch 特征，避免像素重建被颜色映射影响；
3. teacher-student / reverse distillation：学生只学正常特征，异常区域产生特征差异。

对本项目更推荐第 2 种：在冻结 CLIP 的前提下，对正常 patch feature 学一个轻量重建器，不破坏现有 PromptAD 主体。

落地位置：

- 新增 `PromptAD/rf_reconstruction.py`；
- 在 `train_cls.py` 中训练 prompt 的同时训练重建分支，或先固定 prompt 单独训练；
- 推理时输出 `recon_map`，与 `visual_anomaly_map` 融合。

风险：

- 当前 k-shot 很小，像素级 AE 容易欠拟合或过拟合；
- 如果训练样本不是连续频谱帧，预测式模型收益有限；
- 重建误差可能对站点背景变化敏感，需要先做背景归一化。

### 4.6 利用 WASD 等外部 RF 数据进行预训练或 prompt 验证

WASD 包含 LTE/5G 实测背景与 tone/chirp/pulse 合成异常，和当前 burst/chirp 任务高度相关。可以用它做三件事：

1. 验证 RF prompt 是否真的能区分 pulse/chirp/tone；
2. 预训练轻量 adapter 或重建分支；
3. 学习异常结构词和 score map 后处理参数。

注意：如果目标论文实验要求严格少样本和只用目标正常样本，外部数据预训练必须单独作为一组设置报告，不能和纯 few-shot 结果混在一起。

## 5. 推荐的改进优先级

### 第一优先级：图像级分数融合 visual branch

原因：当前代码已经有 patch 级 visual anomaly map，但 Image-AUROC 主要用 textual score。把 `top-k visual score` 融合进 image score 是成本最低、最可能有效的改动。

建议实验：

| 方法 | Text score | Visual top-k | 频率约束 | 说明 |
|---|---:|---:|---:|---|
| baseline | yes | no | no | 当前方法 |
| fusion-v1 | yes | mean top-k | no | 最小改动 |
| fusion-v2 | yes | mean top-k + max | no | 更强调局部异常 |
| fusion-v3 | yes | mean top-k | yes | 加频率位置约束 |

### 第二优先级：结构化 object-agnostic RF prompt

原因：场景内异常检测中，类别名本身仍不是关键语义，应学习 normal/abnormal RF spectrogram，而不是学习地点名。

建议实验：

| Prompt | 说明 |
|---|---|
| legacy | 原 PromptAD 类别 prompt |
| rf | 当前 RF prompt |
| rf_object_agnostic | 去掉站点类别名，只保留 RF 正常/异常 |
| rf_structured | RF 域词 + 异常类型结构词 |
| rf_scene_conditioned | RF 结构词 + 场景背景描述 |

### 第三优先级：频谱背景归一化与信号自适应输入

原因：低 JSR 和不同场景背景条件下，背景差异是主要干扰。输入层做物理合理的归一化，可能比复杂模型更稳。

建议优先实现：

- per-image log-power normalization；
- per-frequency median subtraction；
- `signal_adaptive` 中 DSSS 使用宽带能量/局部方差通道。

### 第四优先级：轻量重建分支

原因：能引入 RF 异常检测文献中的一类强基线，但实现和调参成本高于前几项。

建议先做 feature-level reconstruction，不直接重建彩色图。

## 6. 实验协议建议

为了证明改进有效，建议分三阶段做实验。

### 阶段 A：小规模快速筛选

沿用当前 `run_rf_split_all.py` 的 36 组小批量协议：

- 异常类型：burst / chirp / dsss；
- 场景：4 个站点；
- JSR：m10db / m20db / m30db；
- 指标：Image-AUROC、Pixel-AUROC；
- 每次只改一个因素。

优先比较：

1. `rgb` vs `spectral_gradient` vs `signal_adaptive`;
2. `rf` vs `rf_object_agnostic` vs `rf_structured`;
3. text-only vs text+visual-topk fusion。

### 阶段 B：正式分场景实验

按 scene 分别训练、分别测试：

- `WeaponMuseum_spectrum`
- `Playground_spectrum`
- `TimeSquare_spectrum`
- `Gymnasium_spectrum`

每个异常类型和 ISR 都报告均值，并分别报告 `m10db`、`m20db`、`m30db`。低 ISR 最能体现方法是否真的捕捉弱异常。

### 阶段 C：消融与可视化

必须输出：

- score map 对比：input / GT / baseline / improved；
- 按异常类型统计：burst、chirp、DSSS；
- 按 JSR 统计：越低越难；
- prompt ablation：证明不是 prompt 数量增加带来的偶然收益；
- fusion ablation：证明 visual branch 对 Image-AUROC 有贡献。

## 7. 论文写作角度

如果后续按当前协议整理论文方向，可以把改进方法包装成：

> Frequency-aware PromptAD for Few-shot RF Spectrum Anomaly Detection

核心贡献可以写成三点：

1. 提出频谱任务域的结构化 prompt，将 RF domain semantics、normal background semantics 和 interference structure semantics 组合到 PromptAD；
2. 提出频谱感知输入表示和频率约束正常 patch 记忆库，使模型利用时频图的轴向物理含义；
3. 提出文本异常分数与视觉 patch 异常分数的图像级融合，提升低 JSR 和多场景条件下的检测鲁棒性。

如果实现重建分支，则可以作为第四点：

4. 引入正常频谱背景重建约束，将 one-class RF anomaly detection 的重建误差信号与 VLM prompt 分数融合。

## 8. 参考文献与可借鉴点

| 文献/方法 | 核心思想 | 对 PromptAD 的借鉴 |
|---|---|---|
| CLIP, Radford et al., 2021 | 图文对齐预训练 | PromptAD 文本-图像异常分数基础 |
| CoOp, Zhou et al., 2022 | 可学习 prompt context | 当前 PromptLearner 的理论来源 |
| CoCoOp, Zhou et al., 2022 | 条件化 prompt | 场景/输入条件 prompt |
| WinCLIP, Jeong et al., 2023 | 多模板 prompt、window 特征聚合 | RF prompt ensemble 和 patch 聚合 |
| PromptAD, Li et al., 2024 | 只用正常样本学习异常 prompt | 当前方法主干 |
| AnomalyCLIP, Zhou et al., 2024 | object-agnostic normal/abnormal prompt | 去掉站点类别依赖，关注异常性 |
| PatchCore, Roth et al., 2022 | 正常 patch memory bank + coreset | 改进 visual gallery 和 top-k image score |
| DRAEM, Zavrtanik et al., 2021 | 重建与判别联合异常检测 | 引入正常频谱重建误差 |
| Reverse Distillation, Deng et al., 2022 | teacher-student 特征差异 | 轻量 feature reconstruction 分支 |
| FastFlow, Yu et al., 2021 | flow likelihood 建模正常特征 | 正常 patch 分布建模 |
| SAIFE, Rajendran et al., 2018/2019 | AAE 建模 PSD，学习可解释频谱因子 | 中心频率、带宽、能量分布特征 |
| Deep Predictive Coding RF AD, 2018 | 频谱序列预测误差检测异常 | 有连续帧时加入预测分支 |
| Spectrum Anomaly Detection Based on Spatio-Temporal Network Prediction, Peng et al., 2022 | 多时间尺度频谱预测 | 跨时间背景建模 |
| WASD, Kim et al., 2024 | LTE/5G 背景叠加 tone/chirp/pulse 异常数据集 | 外部 RF 预训练和 prompt 验证 |

## 9. 具体实施清单

短期可做：

- 在 `score_cached` 和 `forward(cls)` 中加入 visual top-k image score；
- 新增 `--score-fusion text|text_visual_topk|text_visual_max`;
- 新增 `rf_object_agnostic` 和 `rf_structured` prompt 模式；
- 对 `spectral_gradient` 增加 per-frequency median subtraction ablation；
- 完成 36 组小批量实验，筛掉无效方案。

中期可做：

- 实现 frequency-window constrained gallery；
- 实现 PatchCore coreset；
- 加入 scene-conditioned prompt；
- 补齐正式分场景实验和 score map 可视化。

长期可做：

- 加 feature-level reconstruction branch；
- 用 WASD 做 RF adapter 或 prompt 预训练；
- 在有连续频谱帧时引入预测式异常检测。

## 10. 总结

当前 PromptAD 频谱分支已经完成了第一步：把频谱图从“普通图片”转成“有 RF prompt 和梯度通道的频谱图”。下一步应把外部文献中的三类思想引入进来：

1. 来自 VLM 异常检测的 object-agnostic、prompt ensemble、条件化 prompt；
2. 来自工业异常检测的正常 patch memory bank、top-k 聚合和重建式异常分数；
3. 来自 RF 频谱异常检测的 PSD/时频结构、中心频率、带宽、噪声底和时空预测建模。

最建议优先落地的是“文本分数 + 视觉 top-k patch 分数融合”和“object-agnostic 结构化 RF prompt”。这两项改动成本低、与当前代码兼容性高，也最容易在 36 组小批量实验中验证是否有效。

## 参考链接

- PromptAD: https://arxiv.org/abs/2404.05231
- WinCLIP: https://arxiv.org/abs/2303.14814
- AnomalyCLIP: https://arxiv.org/abs/2310.18961
- CoOp: https://arxiv.org/abs/2109.01134
- CoCoOp: https://arxiv.org/abs/2203.05557
- CLIP: https://arxiv.org/abs/2103.00020
- SAIFE: https://arxiv.org/abs/1807.08316
- Crowdsourced wireless spectrum anomaly detection: https://arxiv.org/abs/1903.05408
- Deep Predictive Coding Neural Network for RF Anomaly Detection: https://arxiv.org/abs/1803.06054
- Spectrum Anomaly Detection Based on Spatio-Temporal Network Prediction: https://www.mdpi.com/2079-9292/11/11/1770
- WASD dataset page: https://remilab.ai/wasd/wasd-open-dataset/
- WASD paper metadata: https://doi.org/10.1109/ACCESS.2024.3521946
- PatchCore: https://arxiv.org/abs/2106.08265
- DRAEM: https://arxiv.org/abs/2108.07610
- Reverse Distillation: https://arxiv.org/abs/2201.10703
- FastFlow: https://arxiv.org/abs/2111.07677
