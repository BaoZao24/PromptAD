# 从会议讨论提炼的论文写作技巧

这份笔记根据会议中对摘要、方法、实验和图表的逐段讨论整理。原始转写中的英文术语和专有名词有少量识别误差，下面保留讨论中可以迁移到其他论文的写作原则。

## 一、先搭好论文的叙事链

一篇论文的基本逻辑是：

> 研究为什么重要 → 现有方法有什么问题 → 本文提出什么 → 方法为什么能解决问题 → 实验证明了什么

读者在每一段都会追问几个问题：为什么要做？问题到底是什么？你的方法解决了哪一步？凭什么相信它有效？写作时要让这些问题按顺序得到回答，避免让审稿人反复回到前文寻找定义、公式或实验依据。

尤其要把“方法名称”放在逻辑之后。只写“我们提出了某某方法”不能构成贡献，必须同时说明它解决的具体困难，以及它与已有方法的关系。

## 二、摘要可以按五句组织

会议中反复强调的摘要结构可以整理为以下五部分：

1. **意义或背景。** 说明研究对象和应用场景为什么值得研究，尽快把读者带入问题。
2. **问题或缺口。** 指出现有方法在什么条件下失败、受什么瓶颈限制，避免只说“现有方法存在不足”。
3. **核心方案。** 用一句清楚的 `we propose ...` 说明本文提出了什么，以及它针对前一句中的哪个问题。
4. **技术特色。** 用一到两处具体机制解释方法的架构、算法或关键设计如何支撑核心方案。
5. **实验结果。** 说明在哪些数据或实验上验证，并给出能支撑结论的量化结果，例如提升、降低、减少或加速了什么。

可以先用下面的骨架写，再替换成自己的内容：

> 现有方法在 **场景/任务** 中通常采用 **已有做法**，但在 **具体条件** 下会受到 **具体限制**。为解决这一问题，本文提出 **方法/系统**。该方法通过 **关键机制一** 和 **关键机制二**，实现 **与问题直接对应的效果**。在 **数据集/实测数据** 上，本文方法将 **指标** 提升或降低了 **结果**，同时改善了 **资源、速度或质量方面的表现**。

第三句是摘要的“灵魂”：它必须让读者看到“问题—方案”的对应关系。不要在第二句提出问题后，第三句只平淡地报出一个方法名。摘要最后一句如果只是重复前面的实验结果，就不必强行拔高；如果要写“可推广到更多场景”，必须有实验或明确机制支撑，否则可以把这类讨论放到结论中。

## 三、动机和贡献要建立在文献调研上

### 1. 先确认某个做法到底是不是已有方法

会议中对 patch-wise generation 的讨论说明了一个常见风险：把已有方法写成自己的创新，或者没有说明它为什么被引入。写作前要先查清楚：

- 这个方法是否已经在文献中存在；
- 它解决了什么问题；
- 它在当前场景中还留下什么限制；
- 本文到底是在改进、组合还是首次提出。

如果已有方法确实存在，应将其作为已有方法或基线介绍，再明确指出它在当前任务中的失效点。不要为了制造贡献而模糊文献边界。

### 2. 把“现有方法的问题”写具体

好的问题描述应包含“方法—条件—后果”：

> 现有方法在整幅高分辨率输入上直接生成，因此受到显存限制，无法处理更大的输入。

这种写法比“现有方法效率较低”更有说服力，因为读者知道限制发生在哪里、为什么发生，以及后续方法需要解决什么。

### 3. 不要无证据地贬低已有工作

会议中提到，介绍已有工作时要批评它的限制，而不是笼统地“踩别人”。应说明限制的适用范围，并用实验验证本文方法确实改善了这个限制。这样既能建立研究动机，也能避免审稿人认为论断过度。

### 4. 正确使用 `existing`、`classical` 和 `SOTA`

- **existing methods**：泛指已有方法，最稳妥；
- **classical/traditional methods**：强调历史较早、具有代表性的传统方法；
- **state of the art (SOTA)**：只用于当前领域中最新或最强的一类方法。

较早的方法不能因为效果好就称为 SOTA；如果方法已经过时，使用 SOTA 会降低表述可信度。实验中可以同时包含经典方法、近期方法和领域相关方法，但要明确每一类的作用。

## 四、方法部分要让读者少回看

### 1. 第一次出现时定义术语

像 `context`、`neighborhood context`、`valid region`、`overlap region` 这类词不能只靠图猜。第一次出现时要同时说明：它指图中的哪一部分、在算法中承担什么作用、后文是否继续使用。

如果一个词在正文中没有后续作用，就不要为了显得复杂而给它单独命名。术语越多，读者需要记忆的内容越多。

### 2. 区分模块、函数、参数和计算结果

公式中的一个符号必须明确它是什么：

- 一个模块或网络；
- 一个函数；
- 一个参数；
- 一个输入索引；
- 经过计算得到的向量、矩阵或特征。

不能一会儿把同一个符号当作函数，一会儿又当作输出矩阵。若一个位置编码是由索引经过函数计算得到的结果，就要分别定义索引、编码函数和输出，而不是把模块名直接塞进参数表达式。

### 3. 控制缩写和命名

- 缩写第一次出现时给出全称；
- 不要在同一段突然引入多个没有解释的缩写；
- 缩写应尽量短、容易读；
- 如果可以，把方法缩写设计成有实际含义、容易记忆的英文单词；
- 不要为了凑缩写强行从每个词中取字母；
- 同一概念在全文中只使用一种叫法。

方法名的价值是帮助读者记忆，而不是增加术语密度。

### 4. 公式和下标要有统一规则

写完公式后逐项检查：

- 变量、常数、函数和模块的字体是否区分；
- 下标和上标是否真的表达索引、层级或属性；
- 下标是否过长；
- 同一符号在不同公式中含义是否一致；
- 公式中的每个符号是否在附近定义。

会议中建议下标尽量简短。下标一旦过长，公式会变得难读，也容易出现一个符号在不同位置使用不同上下标的问题。

### 5. 算法伪代码要和正文对应

算法中的关键步骤应能在前面的公式或文字中找到对应关系。建议：

- 给关键行加简短注释；
- 用公式编号或步骤编号建立对应；
- 在算法开头或结尾加一句整体说明，例如“Algorithm 1 summarizes the training procedure”；
- 不要让审稿人为了理解一行伪代码，在正文中来回搜索。

伪代码的目的不是重复所有实现细节，而是让读者快速理解输入、核心处理和输出。

## 五、实验部分要回答“凭什么相信”

### 1. 把数据来源说清楚

必须明确区分：

- open-source/public data：来源、名称和获取方式；
- measured/self-collected data：采集对象、地点或场景、样本数量、采集时长和测量条件。

如果数据是实测的，不能只写“we use measured data”。应补充读者判断可信度所需的信息，例如测量设备、硬件型号、软件环境、样本规模和采集条件。实测数据不一定需要放场景照片，但来源和采集过程必须交代。

### 2. 交代可复现实验条件

通信、系统和工程类论文尤其要写清楚：

- GPU/CPU 型号和数量；
- 软件或框架环境；
- 数据来源和划分；
- 关键训练或测试设置；
- 计算开销、内存占用或运行时间的定义。

`computational cost` 和 `computational complexity` 不能在同一篇文章中随意混用。选定一个术语后全文保持一致，并明确它具体测量什么。

### 3. 基线要有代表性

基线数量不足会让审稿人怀疑比较是否公平。选择基线时应考虑：

- 传统或经典方法，用来说明历史方案的能力；
- 近期或强基线，用来说明与当前技术水平的差距；
- 与本文任务和数据场景直接相关的方法；
- 能够对应本文所声称改进点的方法。

不要只列方法名称。正文要告诉读者为什么选它们、它们分别代表哪一类方法，以及比较是否在相同条件下进行。

### 4. 把量化结果写出来

不要只写“效果更好”“性能有提升”。应明确：

- 提升或降低的是哪个指标；
- 改善幅度是多少；
- 在哪些数据或场景上成立；
- 是否伴随速度、内存或质量方面的取舍。

会议中建议用 `in particular` 引出最重要的量化结果：先概括本文相对已有方法的总体优势，再用这一句突出最能证明贡献的指标。

自定义指标也要定义清楚，说明它衡量什么、为什么需要它，并在表格或实验中给出实际数值。不要提出指标却只在文字中口头声称改进。

### 5. 正确区分定性和定量

- 表格中的数值、误差、速度、内存和指标比较属于定量结果；
- 图像中对视觉效果的描述通常属于定性结果；
- 如果图中仍然是在比较数值曲线或统计量，就不能笼统地称为定性结果。

按“定量结果、定性展示、消融实验、资源分析”等实际内容组织实验，比为了凑几个方向而使用模糊分类更清楚。

## 六、图表要帮助读者看懂结论

### 1. 先确认图在真实阅读场景下可读

图不仅要在电脑上放大后能看，也要考虑：

- 黑白打印时是否仍能区分；
- 缩小到论文版面后是否仍能看；
- 手机上查看 PDF 时是否能辨认；
- 不同曲线、区域和算法是否容易混淆。

不能只依赖颜色。应结合实线/虚线、点型、标记、填充方式和文字标签表达差异。

### 2. 给读者视觉提示

如果结论依赖图中的局部区域，应使用箭头、圆圈、放大框、文字标签或不同线型直接标出来。让读者一眼看到“哪里体现了改进”，不要让读者自己在复杂图中寻找。

图中不同区域也应有清晰名称，例如有效区域和重叠区域；名称应与正文、公式和图注一致。

### 3. 一张图只承担清楚的任务

如果多个算法同时放入一张图后无法看出差异，就应删减、拆图、增加局部放大或改用表格。与已有表格表达相同信息的图可以删除。图越多不一定越完整，关键是每张图都要支持一个明确结论。

图注要准确说明图展示的对象、比较关系和指标。不要使用含义模糊的标题，例如只写 `Memory Usage`，却没有说明是比较什么方法、什么输入和什么资源。

## 七、避免没有证据的拔高

会议中特别讨论了摘要或结论中的“推广性”表述。方法如果只在一种数据或一种任务上验证，就不要直接写成“适用于所有数据”或“可用于任意场景”。这类句子会引出新的审稿问题：是否做过其他场景实验？机制是否真的支持推广？

更稳妥的做法是：

- 在正文中只声称实验支持的范围；
- 在结论中说明潜在扩展方向；
- 如果确实声称普适性，就补充跨场景实验或理论依据。

## 八、可以直接使用的投稿前检查表

### 摘要与引言

- [ ] 是否按“意义—问题—方案—机制—结果”组织？
- [ ] 第三句是否明确回答“我们提出什么来解决什么问题”？
- [ ] 问题是否包含具体条件和失败后果？
- [ ] 是否查证了已有方法与本文创新的边界？
- [ ] 是否正确使用 existing、classical 和 SOTA？
- [ ] 是否给出至少一个有意义的量化结果？

### 方法与公式

- [ ] 所有术语和缩写是否在第一次出现时定义？
- [ ] 是否区分模块、函数、参数和输出结果？
- [ ] 下标、上标、字体和符号含义是否前后一致？
- [ ] 公式、正文、图和伪代码是否使用同一套命名？
- [ ] 算法关键步骤是否能对应到公式或文字？
- [ ] 是否删除了对理解方法没有帮助的内部细节？

### 实验与结果

- [ ] 数据是公开的还是实测的，是否写清来源？
- [ ] 实测数据是否交代采集条件、规模和设备？
- [ ] 硬件、软件、GPU 数量和关键设置是否足以复现？
- [ ] 基线是否覆盖经典、近期和任务相关方法？
- [ ] 每个“更好”是否对应具体指标和数值？
- [ ] 自定义指标是否定义并实际展示？
- [ ] 是否明确区分定性展示和定量比较？

### 图表与格式

- [ ] 图在黑白打印、缩小和手机阅读时是否可辨？
- [ ] 是否同时使用颜色、线型、标记或文字说明？
- [ ] 读者能否快速看到结论所在的局部区域？
- [ ] 是否存在信息重复、内容过多或无法辨认的图？
- [ ] 图注是否说明对象、比较关系和指标？
- [ ] 术语如 computational cost/complexity 是否全文统一？

## 最值得保留的一条原则

论文写作不是把方法、公式和结果依次堆上去，而是持续降低读者理解论文的成本。每引入一个术语、公式、图或实验，都要让读者知道它为什么出现、和前文有什么关系、它支持哪个结论。能让审稿人少猜一次、少回看一次，论文的说服力就会提高一层。
