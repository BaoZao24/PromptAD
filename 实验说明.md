# 实验说明

当前项目只保留两套现行实验协议：

1. `RF_SPE_PNG` 公开数据实验
2. 自测四场景 `75/25` 切分实验

下面按协议分别说明数据集路径、训练集和测试集的选取方式。

---

## 一、RF_SPE_PNG 公开数据实验

### 1.1 实验目的

这一组实验用于在公开频谱异常数据上做图像级异常检测。

当前协议的关键点是：

- 正常训练材料来自 `RF_Spectrum_Public_Dataset`；
- 正常测试样本也来自 `RF_Spectrum_Public_Dataset` 中未被用于训练的正常记录；
- 异常测试样本来自 `RF_SPE_PNG/{class_name}/abnormal/{noise_level}`；
- 不做场景划分；
- 不使用 75/25 切分。

### 1.2 支持的异常类型和档位

| 异常类型 | 支持档位 |
|---|---|
| `burst` | `m40db`, `m50db` |
| `chirp` | `m40db`, `m50db` |
| `dsss` | `m40db`, `m50db` |
| `wideband_pulse` | `m20db`, `m30db`, `m40db`, `m50db` |

### 1.3 数据路径

| 数据用途 | 路径 |
|---|---|
| 公开正常数据根目录 | `/mnt/data/wangbei/data/RF_SPE_PNG/RF_Spectrum_Public_Dataset/` |
| 异常图像 | `/mnt/data/wangbei/data/RF_SPE_PNG/{class_name}/abnormal/{noise_level}/` |
| Groundtruth | `/mnt/data/wangbei/data/RF_SPE_PNG/{class_name}/groundtruth/{noise_level}/` |

其中：

- `{class_name}` 取 `burst`、`chirp`、`dsss`、`wideband_pulse`；
- `{noise_level}` 取对应档位；
- `RF_Spectrum_Public_Dataset/` 下的正常图像按 `MeasRes_*` 目录组织；
- 每个 `MeasRes_*` 目录下一般包含一条正常测量记录切出来的多个 patch。

例如：

```text
/mnt/data/wangbei/data/RF_SPE_PNG/RF_Spectrum_Public_Dataset/MeasRes_0770_0004/MeasRes_0770_0004_f20.00-203.00MHz_patch001.png
```

### 1.4 训练集和测试集怎么选

这套协议不是 75/25，而是按正常记录目录划分。

训练集规则：

1. 读取 `RF_Spectrum_Public_Dataset/` 下全部 `MeasRes_*` 目录；
2. 按目录名排序；
3. 取前 `k_shot` 个 `MeasRes_*` 目录作为训练正常集；
4. 这些训练目录中的全部 `.png` patch 都进入训练集。

例如：

- `k_shot = 1`：前 1 个 `MeasRes_*` 目录作为训练集；
- `k_shot = 2`：前 2 个 `MeasRes_*` 目录作为训练集；
- 以此类推。

测试集规则：

1. 剩余所有 `MeasRes_*` 目录中的全部 `.png` patch 作为正常测试样本；
2. `/mnt/data/wangbei/data/RF_SPE_PNG/{class_name}/abnormal/{noise_level}/` 下全部异常图像作为异常测试样本；
3. 测试集 = 剩余正常 patch + 全部该类异常 patch。

因此，这套公开数据实验的划分方式是：

- **训练集：前 `k_shot` 个正常记录目录的全部 patch**；
- **测试集正常部分：剩余正常记录目录的全部 patch**；
- **测试集异常部分：指定异常类型和档位下的全部异常 patch**。

### 1.5 典型运行方式

```bash
python train_cls.py   --dataset rf_spe_png   --class_name burst   --noise-level m50db   --k-shot 1   --split-mode normal_75_25
```

说明：对 `rf_spe_png` 而言，代码当前沿用了 `--split-mode normal_75_25` 这个参数入口，但内部实际执行的是“前 `k_shot` 个正常记录目录训练，剩余正常记录目录测试，再加全部异常样本测试”的公开数据协议，并不做 75/25 切分。

---

## 二、自测四场景 75/25 切分实验

### 2.1 实验目的

这一组实验用于验证方法在自测四场景数据上的表现。特点是：

- 每个场景独立训练、独立测试；
- 正常样本在同一目录内按 `75% / 25%` 切分；
- 异常样本全部进入测试集；
- 训练和测试不跨场景。

### 2.2 数据集与档位

| 数据集 | 场景数 | 档位 |
|---|---:|---|
| `burst_signal` | 4 | `m10db`, `m20db`, `m30db` |
| `chirp_signal` | 4 | `m10db`, `m20db`, `m30db` |
| `dsss_signal` | 4 | `m10db`, `m20db`, `m30db` |
| `wideband_pulse` | 4 | `m20db`, `m30db`, `m40db` |

四个场景是：

- `WeaponMuseum_spectrum`
- `Playground_spectrum`
- `TimeSquare_spectrum`
- `Gymnasium_spectrum`

### 2.3 数据路径

这组实验的真实根目录是下面四套：

| 数据集 | 根目录 |
|---|---|
| `burst_signal` | `/mnt/data/wangbei/data/datasets/burst/` |
| `chirp_signal` | `/mnt/data/wangbei/data/datasets/chirp/` |
| `dsss_signal` | `/mnt/data/wangbei/data/datasets/dsss/` |
| `wideband_pulse` | `/mnt/data/wangbei/data/datasets/wideband_pulse/` |

对任意一个场景 `scene` 和档位 `noise_level`，目录结构都是：

| 数据用途 | 路径 |
|---|---|
| 正常样本目录 | `{root_dir}/{scene}/normal/{noise_level}/` |
| 异常样本目录 | `{root_dir}/{scene}/abnormal/{noise_level}/` |
| Groundtruth | `{root_dir}/{scene}/groundtruth/{noise_level}/` |

### 2.4 75/25 原则怎么切

对每一个 `(dataset, scene, noise_level)` 组合：

1. 读取该目录下全部正常图像；
2. 按文件名排序；
3. 前 `75%` 正常样本作为训练集；
4. 后 `25%` 正常样本作为测试集中的正常部分；
5. 同目录下全部异常图像不参与切分，全部加入测试集。

代码里的实际逻辑是：

```python
n_train = int(n_total * 0.75)
n_train = max(1, min(n_train, n_total - 1))
train_normals = normal_paths[:n_train]
test_normals = normal_paths[n_train:]
```

这表示：

- 75/25 是按文件名排序后的顺序切分，不是随机抽样；
- 至少保留 1 张训练图像；
- 也至少保留 1 张测试图像；
- 如果总数不能被 4 整除，则 `75%` 采用向下取整。

因此这里的原则是：

- **前 75% 正常样本用于训练**；
- **后 25% 正常样本用于测试**；
- **全部异常样本只用于测试**。

### 2.5 训练集和测试集怎么组成

对每个 `(dataset, scene, noise_level)`，最终组成如下：

- 训练集：该场景该档位下前 75% 正常样本；
- 测试集正常部分：该场景该档位下后 25% 正常样本；
- 测试集异常部分：该场景该档位下全部异常样本。

即：

```text
测试集 = 后 25% 正常样本 + 100% 异常样本
```

### 2.6 典型运行方式

```bash
python run_rf_split_all.py --epochs 50
```

如果只想单独跑某个任务，也可以直接调用：

```bash
python train_cls.py   --dataset burst_signal   --class_name WeaponMuseum_spectrum   --noise-level m10db   --split-mode normal_75_25   --normal-train-ratio 0.75
```

---

## 三、两套实验的区别

| 对比项 | RF_SPE_PNG 公开数据实验 | 自测四场景 75/25 切分实验 |
|---|---|---|
| 是否有场景划分 | 没有 | 有，4 个场景独立 |
| 正常样本来源 | `RF_Spectrum_Public_Dataset` 的 `MeasRes_*` 正常记录 | 每个场景自己的 normal 目录 |
| 正常样本切分方式 | 前 `k_shot` 个 `MeasRes_*` 目录训练，剩余目录测试 | 同目录内按 75/25 顺序切分 |
| 训练集 | 前 `k_shot` 个正常记录目录的全部 patch | 前 75% 正常样本 |
| 测试集正常部分 | 剩余正常记录目录的全部 patch | 后 25% 正常样本 |
| 测试集异常部分 | 全部指定 abnormal | 全部 abnormal |
| 是否使用 75/25 原则 | 不使用 | 使用 |
| 当前用途 | 公开数据补充验证 | 主实验 |
