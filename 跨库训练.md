这是一份关于 **AdaptCLIP 跨数据集泛化（Cross-Dataset Generalization）跑库机制** 的详细说明文档。

---

# AdaptCLIP 跨数据集训练与测试机制说明

在传统的无监督异常检测中，模型通常在单个数据集（如纯正常的 MVTec 训练集）上训练，并在同一个数据集的测试集上进行评估。
但 **AdaptCLIP** 采用了一种**跨数据集迁移（Cross-Dataset Transfer）**的研究范式。其核心思想是：利用在**源域（Source Domain）**上的有监督异常数据训练出通用的“异常感知适配器（Adapter）”，然后直接在完全没有见过的**目标域（Target Domain）**上进行零样本（0-shot）或少样本（Few-shot）测试，以验证大模型（CLIP）的泛化能力。

---

## 核心概念：什么是训练集，什么是测试集？

因为这套设定打破了传统界限，我们需要从**源数据集（比如 MVTec）**和**目标数据集（比如 VisA）**两个维度来解释数据的真实去向。

（注：任何标准异常检测数据集在物理磁盘上都分为两部分：`train`(纯正常图像) 与 `test`(包含各种缺陷图像及掩码)。）

### 一、 训练阶段 (Training Phase)
**场景：在源域（如 MVTec）上训练通用 Adapter。**
运行脚本：train_adaptclip.sh (train.py)

在训练阶段，模型需要“学会什么是异常”，因此代码进行了一次强监督训练，其数据喂给方式如下：
* **模型输入 (Query Images)**：使用的是源数据集的 **`test` 划分**。
  * **原因**：这部分数据包含了真实的缺陷图像以及人工标注的完美像素级缺陷掩膜（Ground Truth Mask）。模型利用这些标签计算 Focal Loss 和 Dice Loss，指导 Adapter 学习“如何定位异常区域”。
* **提示输入 (Prompt Images)**：使用的是源数据集的 **`train` 划分**。
  * **原因**：`train` 划分完全是正常图像。每次迭代随机抽取一张正常图像作为 Normal Prompt，帮助网络进行特征对齐（PQ Adapter）。

### 二、 测试阶段 (Testing Phase)
**场景：将训练好的 Adapter 迁移到目标域（如 VisA）上进行评测。**
运行脚本：test_adaptclip.sh (test.py)

测试阶段是检验模型能力的时刻，此时模型**坚决不能**看过目标域的任何异常标签。
* **待测图像 (Query Images)**：使用的是目标数据集的 **`test` 划分**。
  * **原因**：这是常规的测试集，包含未见过的工业品和未见过的缺陷。模型只能输出预测概率图和判定分数，随后与真实的掩码对比，计算最终的 AUROC、AUPRO、F1 等论文评测指标。
* **提示记忆库 (Prompt Memory)**：使用的是目标数据集的 **`train` 划分**（少样本设定）。
  * **原因**：在 K-shot（如 1-shot, 2-shot, 4-shot）模式下，算法允许从目标域的正常数据（`train` 划分）中抽取几张提供给模型作为参考基准。模型把这些极少量的正常图像通过 CLIP 抽取特征并缓存（Prompt Memory），然后再对测试集进行推理。

---

## 具体运行流程图解 (以 MVTec 迁移到 VisA 为例)

假如你想评估模型在 VisA 上的表现，完整的生命周期如下：

### 第 1 步：在源域 MVTec 进行知识提取（强监督训练）
```bash
# 脚本：train_adaptclip.sh
python train.py --dataset mvtec --train_data_path ./dataset/mvtec/
```
* **发生了什么**：模型抓取 MVTec 中的所有物品（电缆、胶囊、坚果等），利用它们身上的**缺陷掩码图**狠狠地训练了 Textual/Visual/PQ Adapter。让模型知道了“异常通常长什么样”。

### 第 2 步：在目标域 VisA 进行泛化测试（零/少样本测试）
```bash
# 脚本：test_adaptclip.sh
train_dataset=mvtec  # 指明用 MVTec 训出来的权重
test_dataset=visa    # 测试目标

python test.py --dataset visa \
               --test_data_path ./dataset/visa/ \
               --k_shots 1 \
               --checkpoint_path ./adaptclip_checkpoints/xxx_train_on_mvtec_xxx/epoch_15.pth
```
* **发生了什么**：加载训练好的权重文件。对于 VisA 中的全新物品（蜡烛、电路板等），如果 `k_shots=1`，就从 VisA 的纯正常数据里拿 1 张图缓存特征；然后去盲跑 VisA 的缺陷图像，输出异常分数。

*(同理，还可以反过来：在 VisA 的 Test 划分上学习，在 MVTec 的 Test 划分上测试评估。)*

---

## 总结：为何这 不是 作弊？

1. **同域作弊 (Data Leakage)**：如果用 MVTec 的 test 训练，然后再测 MVTec 的 test，那就属于彻头彻尾的作弊（模型已经背过了答案）。
2. **跨域泛化 (Generalization)**：AdaptCLIP 的设计是在 A 的 test 结合标签上学经验，在 **毫不相干的 B 的 test 上验证**。这种在不交叉的流形（不同数据集的分布）之间迁移的能力，正是多模态视觉大模型受到学术界追捧的原因，属于合理的 Few-shot / Zero-shot Anomaly Detection 研究。

> **备注提醒**：
> 当前代码库中的 test_adaptclip.sh 底部存在一个由于作者复制粘贴导致的疏漏（第 44-46 行）：
> 评估“用 VisA 训练去测 MVTec”的部分，脚本仍然将 `train_dataset=mvtec` 和 `test_dataset=visa` 写反了，这将导致模型重复执行了一遍第一步的代码。如果是跑库复现，需要把这部分修改为：`train_dataset=visa`, `test_dataset=mvtec`。