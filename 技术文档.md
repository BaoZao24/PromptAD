# PromptAD 中文技术文档

## 1. 项目整体作用

本项目实现的是基于 CLIP 的少样本异常检测方法 PromptAD。核心思想是：

- 使用 CLIP 图像编码器提取频谱图特征；
- 使用可学习 prompt 构造"正常"和"异常"文本特征；
- 同时使用文本分支和视觉分支计算异常分数；
- 最终用 Image-AUROC 或 Pixel-AUROC 评价异常检测效果。

当前分支是**未加入 CLIP-Adapter 的旧方法分支**。该分支中，图像编码器在训练过程中不更新，主要训练的是 `PromptLearner` 中的 prompt 参数。

---

## 2. 主要脚本作用

### 2.1 `train_cls.py`

`train_cls.py` 是**图像级异常分类训练脚本**。

它主要用于计算 Image-AUROC，判断整张频谱图是否异常。

主要流程：

1. 根据命令行参数创建结果目录；
2. 加载训练集和测试集；
3. 创建 `PromptAD` 模型；
4. 用正常训练样本构建视觉特征库 `feature_gallery1` 和 `feature_gallery2`；
5. 缓存测试集图像特征；
6. 训练 prompt 参数；
7. 每个 epoch 评估一次 Image-AUROC；
8. 如果当前结果更好，则保存 checkpoint 和 CSV。

常用命令示例：

```bash
python train_cls.py \
  --dataset rf_open \
  --class_name burst_m10db \
  --k-shot 1 \
  --Epoch 25 \
  --gpu-id 0 \
  --seed 111
```

输出目录示例：

```text
result/rf_open/burst_m10db/k_1/
├── csv/Seed_111-results.csv
├── checkpoint/PromptAD-Seed_111-burst_m10db-check_point.pt
└── imgs/
```

---

### 2.2 `test_cls.py`

`test_cls.py` 是**图像级异常分类测试脚本**。

它不再训练 prompt，而是：

1. 创建 `PromptAD` 模型；
2. 加载 `checkpoint`；
3. 对测试集逐张计算异常分数；
4. 计算并保存 Image-AUROC。

它适合在已有 checkpoint 后单独复现实验结果。

---

### 2.3 `train_seg.py`

`train_seg.py` 是**像素级异常分割训练脚本**。

它主要用于计算 Pixel-AUROC，判断异常区域在图像中的位置。

与 `train_cls.py` 的区别是：

- `train_cls.py` 更关注整张图是否异常；
- `train_seg.py` 更关注异常在图像中的空间位置；
- `train_seg.py` 的异常图会融合文本异常图和视觉异常图。

---

### 2.4 `test_seg.py`

`test_seg.py` 是**像素级异常分割测试脚本**。

作用类似 `test_cls.py`，但指标是 Pixel-AUROC，输出的是异常热力图。

---

### 2.5 `run_rf_open.py`

`run_rf_open.py` 是 **RF Open 数据集批量实验脚本**。

它会自动遍历：

- 信号类型：`burst`、`chirp`、`dsss`
- JSR：`m10db`、`m20db`、`m30db`

总共：

```text
3 × 3 = 9 个任务
```

每个任务都会调用一次 `train_cls.py`。

例如它内部会生成类似命令：

```bash
python train_cls.py \
  --dataset rf_open \
  --class_name burst_m10db \
  --k-shot 1 \
  --Epoch 25 \
  --gpu-id 0 \
  --seed 111
```

跑完后，它会：

1. 读取每个任务的 `Seed_111-results.csv`；
2. 汇总 9 个 Image-AUROC；
3. 打印表格；
4. 调用 `plot_rf_open_heatmap.py` 生成热力图。

---

### 2.6 `run_cross_site_all.py`

`run_cross_site_all.py` 是**跨站点批量实验脚本**。

它固定：

```text
训练站点 = WeaponMuseum_spectrum
```

测试站点包括：

```text
Playground_spectrum
TimeSquare_spectrum
Gymnasium_spectrum
```

数据集包括：

```text
dsss_signal
burst_signal
chirp_signal
```

JSR 包括：

```text
m10db
m20db
m30db
```

所以总任务数是：

```text
3 个数据集 × 3 个测试站点 × 3 个 JSR = 27 个任务
```

该脚本支持多 GPU 并行调度。每个 GPU 对应一个 worker 线程，从任务队列中取任务执行。

常用命令：

```bash
python run_cross_site_all.py --gpus 0 1 2 3 --epochs 50
```

如果只想看将要运行哪些命令，不真正执行，可以用：

```bash
python run_cross_site_all.py --dry-run
```

---

### 2.7 `run_cross_site_burst.py` / `run_cross_site_chirp.py` / `run_cross_site_dsss.py`

这三个脚本是针对单一信号类型的跨站点批量实验脚本。

作用分别是：

- `run_cross_site_burst.py`：只跑 burst；
- `run_cross_site_chirp.py`：只跑 chirp；
- `run_cross_site_dsss.py`：只跑 dsss。

它们比 `run_cross_site_all.py` 更适合做单类信号分析。

---

### 2.8 `plot_heatmap.py`

`plot_heatmap.py` 用于绘制跨站点实验的 AUROC 热力图。

它会读取 `result/` 下的 CSV 文件，把不同：

- 数据集；
- 测试站点；
- JSR；

对应的 AUROC 汇总成图片。

常用命令：

```bash
python plot_heatmap.py --root-dir ./result --seed 111 --out ./result/heatmap_cross_site.png
```

---

### 2.9 `plot_rf_open_heatmap.py`

`plot_rf_open_heatmap.py` 用于绘制 RF Open 数据集的热力图。

它的行通常是：

```text
burst / chirp / dsss
```

列通常是：

```text
m10db / m20db / m30db
```

每个格子显示对应任务的 Image-AUROC。

---

### 2.10 `plot_scoremap.py`

`plot_scoremap.py` 用于可视化异常分数图。

它通常展示：

- 原始输入频谱图；
- ground truth mask；
- 模型生成的 anomaly score map。

这个脚本适合分析模型到底关注了频谱图中的哪些区域。

---

### 2.11 `run_cls.py` 和 `run_seg.py`

这两个是更简单的运行入口脚本，通常用于快速启动分类或分割实验。

- `run_cls.py`：图像级异常分类入口；
- `run_seg.py`：像素级异常分割入口。

如果要做系统实验，推荐优先使用：

- `train_cls.py`
- `run_rf_open.py`
- `run_cross_site_all.py`

---

## 3. 核心模型结构

核心模型在：

```text
PromptAD/model.py
```

主类是：

```python
PromptAD
```

其内部主要包含：

- CLIP 图像编码器；
- CLIP 文本编码器；
- `PromptLearner`；
- 文本特征缓存 `text_features`；
- 正常图像特征库 `feature_gallery1` 和 `feature_gallery2`。

整体数据流如下：

```text
输入频谱图
  ↓
transform 预处理
  ↓
CLIP 图像编码器
  ↓
多路视觉特征
  ├── 全局图像特征：用于图文相似度异常分数
  └── patch 图像特征：用于视觉 memory bank 异常分数
```

文本侧数据流如下：

```text
PromptLearner
  ↓
正常 prompt + 异常 prompt
  ↓
CLIP 文本编码器
  ↓
正常文本特征 + 异常文本特征
```

---

## 4. 训练流程详解

### 4.1 构建正常图像特征库

训练开始前，`train_cls.py` 会先遍历正常训练样本：

```python
_, _, feature_map1, feature_map2 = model.encode_image(data)
```

然后调用：

```python
model.build_image_feature_gallery(features1, features2)
```

在 `PromptAD/model.py` 中，构建方式是：

```python
gallery1 = F.normalize(features1.reshape(-1, d1), dim=-1)
gallery2 = F.normalize(features2.reshape(-1, d2), dim=-1)
```

也就是说，训练集里所有正常样本的 patch 特征会被摊平成一个大的特征库。

这个特征库表示：

```text
正常频谱图的局部 patch 应该长什么样
```

---

### 4.2 训练 prompt

训练时真正优化的是：

```python
model.prompt_learner.parameters()
```

优化器是：

```python
torch.optim.SGD(model.prompt_learner.parameters(), ...)
```

也就是说，当前旧方法不是全量微调 CLIP，而是主要学习 prompt。

训练损失由三部分组成：

```python
loss = loss_v2t + trip_loss + loss_match_abnormal * args.lambda1
```

含义如下：

#### 1. `loss_v2t`

视觉特征到文本特征的交叉熵损失。

目标是让正常图像特征更接近正常文本 prompt，而不是异常文本 prompt。

#### 2. `trip_loss`

三元组损失。

形式上是：

```text
anchor = 图像特征
positive = 正常文本特征
negative = 异常文本特征
```

它希望：

```text
图像特征到正常文本特征的距离 < 图像特征到异常文本特征的距离
```

#### 3. `loss_match_abnormal`

异常 prompt 一致性损失。

代码中有两类异常 prompt：

- 手工异常 prompt；
- 可学习异常 prompt。

该损失希望两类异常 prompt 的平均特征不要偏离太远。

---

### 4.3 为什么测试图像特征可以缓存

在当前旧分支中，图像编码器不训练，视觉特征不会随 epoch 改变。

因此 `train_cls.py` 会在训练前缓存测试集图像特征：

```python
vf = model.encode_image(data_t.to(device))
```

后续每个 epoch 评估时直接调用：

```python
model.score_cached(vf_gpu, 'cls')
```

这样可以避免每个 epoch 重复编码测试图像，显著减少运行时间。

---

## 5. 异常分数如何计算

PromptAD 中异常分数有两条来源：

- **文本异常分数**：图像特征和正常/异常文本特征的相似度；
- **视觉异常分数**：图像 patch 特征和正常图像特征库之间的距离。

这两个分支分别对应：

```python
calculate_textual_anomaly_score()
calculate_visual_anomaly_score()
```

---

## 5.1 文本异常分数

代码位置：

```text
PromptAD/model.py:303-335
```

### 分类任务 `cls`

分类任务使用全局图像特征：

```python
global_feature = visual_features[0]
```

然后计算它与文本特征的相似度：

```python
global_normality_and_abnormality_score = (
    t * global_feature @ self.text_features.T
).softmax(dim=-1)
```

其中：

- `global_feature`：图像全局特征；
- `self.text_features[0]`：正常文本特征；
- `self.text_features[1]`：异常文本特征；
- `t`：CLIP 的温度缩放参数 `logit_scale`；
- `softmax`：把相似度转成概率。

输出有两个概率：

```text
[正常概率, 异常概率]
```

最终取异常类别概率：

```python
global_abnormality_score = global_normality_and_abnormality_score[:, 1]
```

因此分类任务的图像级异常分数可以理解为：

```text
文本异常分数 = P(异常文本 | 图像)
```

数值越大，表示该图像越像异常 prompt。

---

### 分割任务 `seg`

分割任务使用局部 token 特征：

```python
token_features = visual_features[1]
```

每个 patch 都会和正常/异常文本特征做相似度计算：

```python
local_normality_and_abnormality_score = (
    t * token_features @ self.text_features.T
).softmax(dim=-1)
```

然后取每个 patch 的异常概率：

```python
local_abnormality_score = local_normality_and_abnormality_score[:, :, 1]
```

最后 reshape 成二维异常图：

```text
N × 1 × grid_h × grid_w
```

因此分割任务的文本异常图可以理解为：

```text
每个 patch 像不像异常文本描述
```

---

## 5.2 视觉异常分数

代码位置：

```text
PromptAD/model.py:337-348
```

视觉异常分数来自正常图像 feature gallery。

核心代码：

```python
score1, _ = (1.0 - visual_features[2] @ self.feature_gallery1.t()).min(dim=-1)
score1 /= 2.0

score2, _ = (1.0 - visual_features[3] @ self.feature_gallery2.t()).min(dim=-1)
score2 /= 2.0

score = 0.5 * (score1 + score2)
```

### 第一步：计算 patch 与正常库的相似度

由于图像特征和 gallery 特征都做过归一化，所以：

```python
visual_features[2] @ self.feature_gallery1.t()
```

就是余弦相似度。

数值范围大致是：

```text
-1 到 1
```

越接近 1，说明越像正常 patch。

---

### 第二步：把相似度转成距离

代码使用：

```python
1.0 - cosine_similarity
```

如果某个测试 patch 很像正常库里的某个 patch：

```text
cosine_similarity ≈ 1
距离 ≈ 0
```

如果不像正常 patch：

```text
cosine_similarity 较低
距离较大
```

---

### 第三步：取最近的正常 patch

代码使用：

```python
.min(dim=-1)
```

含义是：

```text
对每个测试 patch，在正常 gallery 中找一个最像它的正常 patch
```

如果连最像的正常 patch 都不像它，那么这个 patch 就更可能异常。

---

### 第四步：两路 patch 特征平均

模型有两路 patch 特征：

- `visual_features[2]`
- `visual_features[3]`

它们分别对应：

- `feature_gallery1`
- `feature_gallery2`

最终视觉异常分数是二者平均：

```python
score = 0.5 * (score1 + score2)
```

所以视觉异常分数可以理解为：

```text
视觉异常分数 = 当前 patch 到正常 patch 库的最近距离
```

距离越大，越异常。

---

## 5.3 图像级分类最终分数

在 `task == 'cls'` 时，代码流程是：

```python
textual_anomaly = self.calculate_textual_anomaly_score(visual_features, 'cls')
visual_anomaly_map = self.calculate_visual_anomaly_score(visual_features)
```

然后视觉异常图会被插值到输出大小：

```python
anomaly_map = F.interpolate(
    visual_anomaly_map,
    size=(self.out_size_h, self.out_size_w),
    mode='bilinear',
    align_corners=False
)
```

最后返回：

```python
return am_img_list, am_pix_list
```

其中：

- `am_img_list`：图像级异常分数；
- `am_pix_list`：像素级异常热力图。

需要注意：

**当前分类任务的 Image-AUROC 主要使用 `am_img_list`，也就是文本异常分数。**

视觉分支生成的 `am_pix_list` 主要用于辅助计算、可视化和部分指标分析。

---

## 5.4 像素级分割最终分数

在 `task == 'seg'` 时，代码会同时计算：

```python
textual_anomaly_map = self.calculate_textual_anomaly_score(visual_features, 'seg')
visual_anomaly_map = self.calculate_visual_anomaly_score(visual_features)
```

然后使用如下方式融合：

```python
anomaly_map = 1. / (1. / textual_anomaly_map + 1. / visual_anomaly_map)
```

这相当于一种调和式融合。

直观理解：

- 文本分支认为异常；
- 视觉分支也认为不像正常 patch；
- 融合分数才会更高。

之后异常图会被上采样到目标分辨率：

```python
F.interpolate(anomaly_map, size=(out_size_h, out_size_w), mode='nearest')
```

并经过高斯平滑：

```python
gaussian_filter(am_pix[i], sigma=4)
```

最终得到像素级 anomaly score map。

---

## 6. 指标如何计算

### 6.1 Image-AUROC

Image-AUROC 用于图像级异常分类。

输入是：

```text
每张测试图的异常分数 scores_img
每张测试图的真实标签 gt_list
```

如果异常图整体分数越高、正常图整体分数越低，则 Image-AUROC 越高。

在代码中由：

```python
metric_cal_img(np.array(scores_img), gt_list, np.array(score_maps))
```

计算。

---

### 6.2 Pixel-AUROC

Pixel-AUROC 用于像素级异常分割。

输入是：

```text
每个像素的异常分数 score_maps
每个像素的真实 mask gt_mask_list
```

如果异常区域像素分数高、正常区域像素分数低，则 Pixel-AUROC 越高。

在代码中由：

```python
metric_cal_pix(np.array(score_maps), gt_mask_list)
```

计算。

---

## 7. Checkpoint 保存了什么

在 `train_cls.py` 中，checkpoint 只保存三个关键内容：

```python
selected_keys = [
    'feature_gallery1',
    'feature_gallery2',
    'text_features',
]
```

也就是说，旧方法保存的是：

- 正常图像 patch 特征库 1；
- 正常图像 patch 特征库 2；
- 训练后的正常/异常文本特征。

当前旧分支不保存 CLIP backbone 参数，也不保存 adapter 参数。

原因是：

- CLIP backbone 使用预训练权重；
- 旧方法训练重点是 prompt；
- 最终推理需要的是已经构建好的 gallery 和 text features。

---

## 8. 结果目录结构

结果目录由：

```text
utils/training_utils.py:get_dir_from_args()
```

统一生成。

### RF Open 数据集

```text
result/rf_open/{class_name}/k_{shot}/
├── csv/
├── checkpoint/
└── imgs/
```

例如：

```text
result/rf_open/dsss_m30db/k_1/
├── csv/Seed_111-results.csv
├── checkpoint/PromptAD-Seed_111-dsss_m30db-check_point.pt
└── imgs/
```

### 跨站点数据集

如果指定了训练站点 `train_site`，目录名会变成：

```text
{train_site}_to_{test_site}
```

例如：

```text
result/dsss_signal/WeaponMuseum_spectrum_to_Playground_spectrum/m10db/k_24/
├── csv/Seed_111-results.csv
├── checkpoint/
└── imgs/
```

---

## 9. 为什么 chirp / burst 效果通常更好，而 dsss 更差

从异常分数计算方式可以看出，当前 PromptAD 更擅长识别：

- 视觉上明显的局部结构；
- 与正常 patch 差异很大的局部异常；
- 能被文本 prompt 明确描述的形状。

因此：

- `chirp` 有明显斜线；
- `burst` 有明显短时突发；
- 它们更容易被 CLIP patch 特征和 prompt 捕获。

但 `dsss` 是宽带弥散弱信号：

- 单个 patch 和正常底噪很像；
- 异常更多体现为整体分布轻微变化；
- 与正常 gallery 的余弦距离不一定大；
- 文本 prompt 也很难用简单形状词描述。

所以当前异常分数机制对 `dsss` 天然更困难。

---

## 10. 总结

当前旧分支的 PromptAD 可以概括为：

```text
CLIP 提图像特征
+ PromptLearner 学正常/异常文本特征
+ 正常 patch feature gallery 建模视觉正常性
+ 文本异常分数和视觉异常图共同完成异常检测
```

其中异常分数的核心是：

```text
文本异常分数 = 图像特征更像异常文本 prompt 的概率
视觉异常分数 = 图像 patch 到正常 patch gallery 的最近距离
```

分类任务主要看文本异常分数，分割任务会融合文本异常图和视觉异常图。

这套机制对 `chirp`、`burst` 这类强视觉结构异常效果较好；对 `dsss` 这类弱信号、弥散异常，需要进一步引入频谱统计、全局建模或分布建模方法。
