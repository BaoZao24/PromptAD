# 工程写作检查与待办

依据：engineering-paper-writing。检查对象：现有方案介绍.md、SpectraMemAD_English.md及其PDF。日期：2026-09-08。

## 本轮已改

- 摘要按应用需求、少样本限制、核心方案、双尺度记忆与融合机制、量化结果推进。保留“从参数学习转向记忆学习”，未增加未经实验支持的效率或推广性结论。
- 英文引言移除贡献前重复的范式口号，贡献仍以We开头；性能概括明确限定为比较方法和AUROC。
- 方法明确每张图产生空间特征向量及其汇总矩阵，补充余弦距离、正常分数百分位、温度、分位门槛和修正强度的含义。保持原有公式。
- 补充VAE、CA-CFAR等缩写解释；英文实验介绍按统计检测、频谱方法、视觉方法说明基线的角色。
- 英文消融文字明确同一数据划分下比较单分支与完整模型。现有消融支持联合评分，不据此声称已经单独验证校准或门槛的作用。
- 实验表格、数值、参考文献和方法实现不变。英文PDF为5页，中文PDF为6页；未通过缩小字体或页边距压缩篇幅。

## 仍需补充或核实的证据

| 优先级 | 位置与问题 | 下一步 |
| --- | --- | --- |
| 高 | 自采数据仅简要说明来源；设备、采集时长、样本规模及测量条件不完整 | 从采集记录补齐真实信息，正文保留足以判断数据可信度的几项；不能拿当前机器配置代替历史实验配置。 |
| 高 | deceptive的功率参数用dB表示，但正文没有说明参考功率；中文FedJam表中的10/20/30 dB也与英文叙述未形成一致解释 | 对照数据生成脚本和原始数据说明确认每个参数的物理含义，再统一单位和文字。当前未猜测改成dBm，也未删除原数值。 |
| 高 | Public RF文献身份已确认，但与实际下载数据的对应关系尚未闭合 | 追踪下载路径、文件说明、数据清单与文献中的测量活动。 |
| 高 | 基线适配、训练终止和参数选择流程没有完整交代 | 从正式运行配置核对目标场景训练数据、模型选择规则，以及TFAM图像分支、GRETEL频带图的适配方式。图像输入协议不能写成完整复现原文所有模态。 |
| 高 | 图像级ViT分数汇总规则仍未完整写入实现细节 | OFDMA脚本的patch_score_rank=3已确认；需进一步对照当前表格对应输出，核对RF和FedJam是否使用最大值及其具体配置，再一并写入正文。 |
| 高 | 少样本稳定性没有在当前主表体现 | 旧《论文实验部分》同时含多种子结果和旧TTA/融合设置，先核对版本与来源，不能直接把旧标准差加到新结果上。 |
| 中 | “正常参考分布”在1-shot时如何形成足够的参考分数，需要按观测单位说明 | 特征级排除自匹配已核对，但聚合后参考分数的数量、单参考分数时的尺度回退，仍需与各数据集正式执行路径核对。 |
| 中 | 融合贡献缺少独立验证 | 单分支对比支持联合使用；若进一步声称分位门槛或稳健校准带来收益，需要保持其他设置不变的对照。 |
| 中 | 固定融合参数的开发依据不充分 | 核对开发数据是否与最终测试独立，不把“只使用正常参考计算统计量”等同于“所有超参数也只由正常参考选出”。 |
| 中 | 免训练没有相应资源量化 | 可记录正常记忆构建时间、单张推理延迟和记忆存储；需要统一硬件、批量和计时边界，不从冻结参数直接推断速度更快。 |

## 图表检查

- 当前架构图保持用户原版，候选A/B/C尚未选定，因此不替换。原图的Gallery与正文Memory、输出S与正文s(x)待选图后统一。
- k-shot ROC绘图脚本里GRETEL使用点划线，但TFAM-AAE、SPADE和SpectraMemAD仍都是实线。后续制图可增加虚线或间隔标记，改善黑白打印辨识度。
- 当前ROC图仅含代表性方法；PatchCore是主表的最强AUROC基线，却未出现在这些k-shot曲线中。若其逐样本分数可用，优先补上PatchCore曲线，不能由AUROC标量推造ROC。
- 小图和消融表缩放后字体较密。后续若增加实验内容，应优先调整信息布局和重复说明，保留可读字号。

以上待办是证据与排版检查结果，不属于已经完成的新实验，也不应直接作为防御性说明全部放入论文。
