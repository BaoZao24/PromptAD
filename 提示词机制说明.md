# PromptAD 提示词机制说明

本文档解释当前项目中 prompt / PromptLearner 的完整工作流程，重点说明：

```text
prompt 文本从哪里来
PromptLearner 学了什么
训练时 prompt 怎么被优化
推理时 prompt 怎么变成异常分数
为什么单纯增加特征词收益有限
```

相关代码主要在：

```text
PromptAD/ad_prompts.py
PromptAD/model.py
train_cls.py
```

## 1. 总体流程

当前模型使用 CLIP 式视觉-文本对齐。图像进入 CLIP image encoder，prompt 进入 CLIP text encoder。模型最终比较图像特征和文本特征的相似度，得到正常 / 异常概率。

简化流程如下：

```text
类别名 / 数据集名
        |
        v
ad_prompts.py 选择 prompt_mode 对应的类别名和异常模板
        |
        v
PromptLearner 构造 normal prompt / abnormal prompt
        |
        v
把普通词 token 固定，把 N / A 占位 token 替换成可学习向量
        |
        v
CLIP text encoder 编码 prompt embedding
        |
        v
多条 normal prompt 平均成 normal 文本原型
多条 abnormal prompt 平均成 abnormal 文本原型
        |
        v
图像特征分别和 normal / abnormal 文本原型做相似度
        |
        v
softmax 后取 abnormal 概率作为文本异常分数
```

需要注意：当前最终推理只保留两个文本原型：

```text
text_features[0] = normal prototype
text_features[1] = abnormal prototype
```

也就是说，所有异常描述词最终会被平均到同一个 abnormal prototype 中。

## 2. prompt_mode 如何决定文本内容

`PromptAD/ad_prompts.py` 里定义了频谱任务的 prompt 模板。

当前训练脚本支持这些 prompt mode：

```text
generic
rf_domain
rf
legacy
rf_object_agnostic
rf_scene_conditioned
rf_signal_structured
```

### 2.1 数据集到信号类型的映射

频谱数据集会先被映射成信号类型：

```python
rf_signal_dataset_mapping = {
    "burst_signal": "burst",
    "chirp_signal": "chirp",
    "dsss_signal": "dsss",
    "deceptive_signal": "deceptive",
    "wideband_pulse": "wideband",
}
```

这个映射用于判断应该使用 burst / chirp / dsss / wideband 等哪一组异常描述。

### 2.2 类别名如何生成

`get_prompt_classname()` 负责生成 prompt 里的主体名。

如果是普通 `rf` 模式，频谱类会统一成：

```text
radio frequency spectrogram
```

如果是 `rf_signal_structured`，会进一步带上信号类型：

```text
burst signal radio frequency spectrogram
chirp signal radio frequency spectrogram
DSSS spread-spectrum radio frequency spectrogram
wideband pulse radio frequency spectrogram
```

如果是 `generic`，则退化成：

```text
image
```

### 2.3 异常模板如何生成

`get_abnormal_prompt_states()` 负责返回异常描述模板。

普通 `rf` 模式会先使用通用 RF 异常词：

```text
abnormal {}
{} with anomalous signal energy
{} with unexpected interference
{} with injected radio-frequency interference
{} with abnormal time-frequency structure
{} with signal energy inconsistent with the normal background
```

然后如果不是 `rf_object_agnostic`，还会追加 burst / chirp / dsss 等信号类型专属模板。

`rf_signal_structured` 则直接使用更结构化的信号类型模板。例如 chirp：

```text
{} with broken diagonal chirp trace
{} with distorted frequency-sweeping slope
{} with interrupted slanted time-frequency streak
{} with abnormal chirp slope angle
{} with faint low-power chirp interference
{} with spurious diagonal RF artifact
```

这些模板会通过 `.format(classname)` 变成完整句子。

## 3. PromptLearner 学了什么

`PromptLearner` 在 `PromptAD/model.py` 中定义。它不是直接学习一段人类可读的英文，而是学习 CLIP 文本 embedding 空间里的若干可训练 token 向量。

当前默认超参来自 `train_cls.py`：

```text
n_ctx = 4
n_pro = 3
n_ctx_ab = 1
n_pro_ab = 4
```

含义如下：

| 参数 | 含义 |
|---|---|
| `n_ctx` | normal prompt 前面的可学习上下文 token 数 |
| `n_pro` | normal prompt 的组数 |
| `n_ctx_ab` | learned abnormal prompt 中额外异常 token 数 |
| `n_pro_ab` | learned abnormal prompt 的组数 |

PromptLearner 内部有两类可学习参数：

```text
normal_ctx   : shape = (n_pro, n_ctx, ctx_dim)
abnormal_ctx : shape = (n_pro_ab, n_ctx_ab, ctx_dim)
```

默认情况下：

```text
normal_ctx   有 3 组，每组 4 个可学习 token
abnormal_ctx 有 4 组，每组 1 个可学习 token
```

其中 `ctx_dim` 是 CLIP text token embedding 的维度。

## 4. 三类 prompt 是怎么构造的

PromptLearner 会构造三类 prompt：

```text
normal_prompts
abnormal_prompts_handle
abnormal_prompts_learned
```

### 4.1 normal_prompts

normal prompt 的文本形式类似：

```text
N N N N radio frequency spectrogram.
```

这里的 `N` 只是占位符。tokenize 之后，`N` 对应位置会被 `normal_ctx` 的可学习向量替换。

默认 `n_pro=3`，所以会生成 3 条 normal prompt。它们文本表面一样，但内部使用 3 组不同的 `normal_ctx`。

### 4.2 abnormal_prompts_handle

手工异常 prompt 的文本形式类似：

```text
N N N N abnormal radio frequency spectrogram.
N N N N radio frequency spectrogram with abnormal time-frequency structure.
N N N N radio frequency spectrogram with broken diagonal chirp trace.
```

这类 prompt 的异常语义主要来自 `ad_prompts.py` 中的人写模板。

注意：它前面仍然使用 `normal_ctx`，没有使用 `abnormal_ctx`。也就是说，手工异常 prompt 的可学习部分和 normal prompt 共享 normal 上下文。

如果某个 prompt mode 返回 `K` 条异常模板，则手工异常 prompt 数量是：

```text
K * n_pro
```

因为每条异常模板都会和每一组 `normal_ctx` 组合。

### 4.3 abnormal_prompts_learned

可学习异常 prompt 的文本形式类似：

```text
N N N N A radio frequency spectrogram.
```

其中：

```text
N 位置使用 normal_ctx
A 位置使用 abnormal_ctx
```

这部分没有显式写入 “broken diagonal line” 或 “wideband texture” 等异常词，而是让 `abnormal_ctx` 在训练中学习一个异常方向。

默认数量是：

```text
n_pro_ab * n_pro = 4 * 3 = 12 条
```

## 5. token 替换机制

PromptLearner 初始化时会先把完整字符串送入 CLIP tokenizer，例如：

```text
N N N N radio frequency spectrogram.
N N N N abnormal radio frequency spectrogram.
N N N N A radio frequency spectrogram.
```

然后取出 CLIP 原始 token embedding，并把序列切成：

```text
prefix = SOS token
ctx    = N / A 对应的可学习 token 位置
suffix = 类别名、异常描述、标点、EOS 等固定 token
```

真正 forward 时拼接的是 embedding 序列，而不是重新拼字符串：

```text
normal prompt:
[SOS] + normal_ctx + fixed_suffix

handle abnormal prompt:
[SOS] + normal_ctx + fixed_abnormal_suffix

learned abnormal prompt:
[SOS] + normal_ctx + abnormal_ctx + fixed_suffix
```

之后这些 embedding 序列进入 CLIP text encoder，得到文本特征。

## 6. 训练时 prompt 怎么被优化

训练入口在 `train_cls.py`。

模型首先冻结 CLIP 主体：

```text
CLIP image encoder 冻结
CLIP text encoder 冻结
PromptLearner 可训练
可选 visual adapter / visual LoRA 可训练
```

默认主要训练的是 `PromptLearner`。

每个 batch 的训练步骤如下：

```text
1. 用 PromptLearner 生成 normal / abnormal prompt embedding
2. 用 CLIP text encoder 得到 normal_text_features
3. 用 CLIP text encoder 得到 abnormal_text_features_handle
4. 用 CLIP text encoder 得到 abnormal_text_features_learned
5. 用 CLIP image encoder 得到正常训练图像的 cls_feature
6. 让正常图像更接近 normal prompt
7. 让正常图像远离 abnormal prompt
8. 让 learned abnormal 和 handle abnormal 的平均方向保持接近
```

训练损失由三部分组成：

```text
loss = loss_v2t + trip_loss + lambda1 * loss_match_abnormal
```

### 6.1 loss_v2t

`loss_v2t` 是图像到文本的分类损失。

对每张正常训练图像，模型构造 logits：

```text
[ normal_similarity, abnormal_similarity_1, abnormal_similarity_2, ... ]
```

训练目标固定为第 0 类，也就是 normal。

直观理解：

```text
正常训练图像应该更像 normal prompt，
不应该像任何 abnormal prompt。
```

### 6.2 trip_loss

`trip_loss` 使用正常图像特征、normal 文本中心、abnormal 文本中心。

目标是：

```text
正常图像靠近 normal 文本中心
正常图像远离 abnormal 文本中心
```

### 6.3 loss_match_abnormal

`loss_match_abnormal` 约束：

```text
手工异常 prompt 的平均方向
和
可学习异常 prompt 的平均方向
```

二者不要差太远。

直观理解：

```text
abnormal_ctx 学到的异常方向，应该和人工异常模板提供的方向保持一致。
```

默认 `lambda1=0.001`，所以这是一个较弱的辅助约束。

## 7. 每个 epoch 后如何构建文本原型

每个 epoch 训练完后，代码会调用：

```python
model.build_text_feature_gallery()
```

它会重新用当前 PromptLearner 参数生成文本特征，然后做平均：

```text
normal_text_features   -> mean -> normal prototype
abnormal_text_features -> mean -> abnormal prototype
```

最终写入：

```text
self.text_features[0] = normal prototype
self.text_features[1] = abnormal prototype
```

这一步非常关键：无论前面有多少条异常模板，推理阶段默认都会被压缩成一个 abnormal prototype。

## 8. 推理时如何得到异常分数

分类任务中，图像级异常分数主要来自文本分支。

流程如下：

```text
1. 图像进入 CLIP image encoder
2. 得到 global_feature
3. global_feature 和 normal / abnormal 两个文本原型做点积
4. 经过 softmax 得到 [normal_prob, abnormal_prob]
5. abnormal_prob 作为图像级异常分数
```

对应逻辑是：

```text
global_normality_and_abnormality_score =
    softmax(logit_scale * global_feature @ text_features.T)

global_abnormality_score =
    global_normality_and_abnormality_score[:, 1]
```

当前 `cls-score-mode=text_only` 时，Image-AUROC 使用的就是这个文本异常分数。

模型也会计算视觉异常图：

```text
测试图像 patch 特征
    vs
正常训练样本 patch feature gallery
```

如果某个 patch 和正常 gallery 都不像，则视觉异常图响应较高。

但在默认分类设置下，视觉异常图主要用于 score map / 可视化，不直接决定 Image-AUROC。除非显式使用：

```text
--cls-score-mode visual_topk
--cls-score-mode visual_topk_max
--cls-score-mode visual_topk_freq
```

## 9. 为什么单纯加特征词可能没效果

结合当前实现，prompt 词汇实验收益有限有几个直接原因。

### 9.1 所有异常词最终会被平均

burst、chirp、dsss 的结构词最终都会进入同一个 abnormal prototype。

例如：

```text
short-duration narrow pulse
diagonal chirp trace
diffuse wideband texture
noise-like spectral pattern
rectangular wideband block
```

这些词描述的是不同形态，但最终平均后只剩一个异常中心。词越多，越可能互相稀释。

### 9.2 训练只有正常图像

PromptLearner 训练时主要看到正常样本。异常词没有真实异常图像来对齐，所以这些词更多是提供“远离正常”的语义方向，而不是精确学习 burst / chirp / dsss 的形态。

### 9.3 CLIP 对 RF 专业词的理解有限

CLIP 对自然图像词汇更熟悉。像 `DSSS`、`spread-spectrum`、`radio-frequency interference` 这类专业词未必有稳定视觉语义。

因此更有效的词往往不是专业术语，而是视觉形态词：

```text
diagonal line
slanted streak
narrow pulse
vertical stripe
wideband texture
uneven density
noise-like band
faint trace
broken trace
```

### 9.4 当前主提升主要来自输入特征

历史结果中，`rf_signal_structured` 相比 `rf` 的收益很小；而输入增强如 `signal_adaptive`、`morph_fusion_gray_residual_a01` 的收益明显更大。

这说明当前瓶颈更多在视觉特征表达，而不是 prompt 文案本身。

## 10. 当前机制的优点和限制

### 优点

```text
实现简单，和原 PromptAD 框架兼容
只用正常样本即可训练
手工异常模板和可学习 abnormal_ctx 可以互相约束
推理只需要 normal / abnormal 两个文本原型，计算成本低
```

### 限制

```text
不同异常形态被合并成一个 abnormal prototype
异常词之间没有独立投票机制
无法判断当前图像更像 burst / chirp / dsss 哪一类异常形态
增加大量 prompt 可能稀释异常方向
RF 专业词不一定能被 CLIP 稳定理解
```

## 11. 对后续 prompt 改进的启发

如果后续继续改 prompt，建议优先改机制，而不是继续扩充词表。

更合理的方向是：

```text
normal prototype 保持一个或少数几个
abnormal prototype 按形态分组保留多个
推理时让多个 abnormal prototype 独立打分
最终用 max / top-k / 加权融合得到异常分数
```

例如：

```text
normal prototype:
  clean radio frequency spectrogram
  regular time-frequency background

burst abnormal prototype:
  short-duration narrow pulse
  transient vertical energy stripe

chirp abnormal prototype:
  diagonal time-frequency streak
  slanted frequency-sweeping trace

dsss abnormal prototype:
  diffuse wideband texture
  uneven noise-like spectral band
```

然后用：

```text
score_burst = sim(image, burst_abnormal) - sim(image, normal)
score_chirp = sim(image, chirp_abnormal) - sim(image, normal)
score_dsss  = sim(image, dsss_abnormal)  - sim(image, normal)

final_score = max(score_burst, score_chirp, score_dsss)
```

这会比把所有异常词平均成一个 abnormal prototype 更符合当前频谱异常的实际结构。

